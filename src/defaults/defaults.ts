import { defaultWebGLCanvasContext } from "./defaultWebGLCanvasContext";
import { defaultWebGLPassDescriptor } from "./defaultWebGLPassDescriptor";

/**
 * 默认值
 */
export const defaults = Object.freeze({
    webGLCanvasContext: defaultWebGLCanvasContext,
    webGLPassDescriptor: defaultWebGLPassDescriptor,
});